import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemycourse.ionicbookmd',
  appName: 'ionic-booking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
